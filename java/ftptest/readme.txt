This project contains a FTP that receives files.
They are stored in a home dir.
Next they are copied to a rundir. 
If the file is a zip, it is also unzip.

The idea was to build an application that next could launch
the application contained in the zip. This step was never implemented.